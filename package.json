{
  "name": "clipit",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-browser WebExtension for clipping the currently selected video.",
  "scripts": {
    "build": "node scripts/build.js all",
    "build:chrome": "node scripts/build.js chrome",
    "build:firefox": "node scripts/build.js firefox",
    "test": "node --test"
  }
}
